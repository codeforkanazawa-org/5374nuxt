/*
 * 5374 setting
 *
*/

export const SVGLabel = true; // SVGイメージを使用するときは、true。用意できない場合はfalse。

export const MaxDescription = 9; // ごみの最大種類、９を超えない場合は変更の必要はありません。

export const MaxMonth = 3;

export const WeekShift = true; // 休止期間なら週をずらすときは、true。金沢の仕様は、true。

export const SkipSuspend = true; // 休止期間を除去するときは、true。奈良の仕様は、true。
